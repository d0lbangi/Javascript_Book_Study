// === 서로 같은지 엄격하게 검사
// !== 서로 다른지 엄격하게 검사

// == 서로 같은지 느슨하게 검사
// != 서로 다른지 느슨하게 검사

let count = 0;
count +=1;
count ++; // 증가 연산자

console.log(count);

// && => and => 그리고
// || => or => 또는

const temp = 1 === '1' ? '참일때' : '거짓일때';

if (1==='1') {
    temp1 = '참일때';
} else {
    tem2 = '거짓일때';
}


